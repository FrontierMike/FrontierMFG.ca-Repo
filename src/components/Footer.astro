---
import { business, telHref } from '../data/site';

interface Props {
  page?: 'home' | 'services' | 'lightwell';
}
const { page = 'home' } = Astro.props;
const p = page === 'home' ? '' : '/';
const year = new Date().getFullYear();

const meta = 'font-family:\'IBM Plex Mono\',monospace;font-size:12px;color:var(--color-body);line-height:1.7;';
---
<footer style="background:var(--color-bg-deep);color:var(--color-muted);border-top:1px solid rgba(255,255,255,0.06);">
  <div style="max-width:1200px;margin:0 auto;padding:40px 32px;display:flex;flex-wrap:wrap;gap:28px;align-items:flex-start;justify-content:space-between;">
    <a href="/" style="display:flex;align-items:center;gap:10px;color:var(--color-body);font-weight:700;font-size:16px;">
      <span style="display:inline-block;width:9px;height:9px;background:var(--color-accent);border-radius:2px;transform:rotate(45deg);"></span>
      FRONTIER<span style="color:var(--color-accent-text);">&nbsp;MFG</span><span style="color:var(--color-body);font-weight:400;">&nbsp;Services</span>
    </a>
    <nav aria-label="Footer" style="display:flex;flex-wrap:wrap;gap:24px;">
      <a href="/services" style="color:var(--color-muted);font-size:14px;">Services</a>
      <a href="/lightwell" style="color:var(--color-muted);font-size:14px;">Lightwell</a>
      <a href={`${p}#process`} style="color:var(--color-muted);font-size:14px;">Process</a>
      <a href={`${p}#service-area`} style="color:var(--color-muted);font-size:14px;">Service area</a>
      <a href={`${p}#about`} style="color:var(--color-muted);font-size:14px;">About</a>
      <a href={`${p}#contact`} style="color:var(--color-muted);font-size:14px;">Contact</a>
    </nav>

    <!-- Name / address / phone, repeated on every page. Matches the contact
         section and the LocalBusiness schema exactly — see src/data/site.ts. -->
    <address style={meta + 'font-style:normal;'}>
      <span style="color:var(--color-muted);">{business.legalName}</span><br />
      {business.locality}, {business.regionName}, Canada<br />
      <a href={`mailto:${business.email}`} style="color:var(--color-body);">{business.email}</a>
      {business.phone && <> · <a href={telHref} style="color:var(--color-body);">{business.phone}</a></>}
    </address>
  </div>
  <div style="border-top:1px solid rgba(255,255,255,0.06);">
    <div style={'max-width:1200px;margin:0 auto;padding:18px 32px;display:flex;flex-wrap:wrap;gap:8px 20px;justify-content:space-between;' + meta}>
      <span>© {year} {business.legalName}</span>
      <span>Serving Vancouver, the Lower Mainland &amp; the Fraser Valley</span>
    </div>
  </div>
</footer>
